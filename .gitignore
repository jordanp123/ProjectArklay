# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Dependencies / build output (this project has none, but just in case)
node_modules/
dist/
build/

# Archives / local scratch
*.zip
*.log
_*.html

# Secrets — the Cloudflare Tunnel token is injected at runtime, never committed
.env
.env.*
